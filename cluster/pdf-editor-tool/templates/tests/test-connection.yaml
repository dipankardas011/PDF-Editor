apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pdf-editor-tool.fullname" . }}-test-connection"
  labels:
    {{- include "pdf-editor-tool.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pdf-editor-tool.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
