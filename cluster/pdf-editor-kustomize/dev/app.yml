apiVersion: apps/v1
kind: Deployment
metadata:
  name: pdf-editor-web
  labels:
    web: pdf-editor
spec:
  replicas: 2
  selector:
    matchLabels:
      web: pdf-editor
  template:
    metadata:
      labels:
        web: pdf-editor

    spec:
      containers:
        - name: pdf-editor
          image: dipugodocker/pdf-editor:0.5v
          env:
          - name: PORT
            valueFrom:
              configMapKeyRef:
                name: pdf-config
                key: PORT
          resources:
            requests:
                memory: "50Mi"
                cpu: "100m"
            limits:
              memory: "400Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 5680 # as we are not specifiing the $PORT so default is 8080
          ports:
            - containerPort: 5680
          imagePullPolicy: Always
...