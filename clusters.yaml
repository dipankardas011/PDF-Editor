#### kubernetes ####
apiVersion: v1
kind: Pod
metadata:
  name: PDF-editor
  labels:
    name: PDF-editor
spec:
  containers:
  - name: PDF-editor
    image: <Image>
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 8080
